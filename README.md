# gpt2_learning_model

A module to fine-tune GPT-2 model by your own dataset.
The program is in development.
#
# This application is designed to provide an easy-to-use interface for fine-tuning pre-trained models, helping students quickly learn the fundamentals of neural network fine-tuning during workshops.

